# ========================================
# PDF to Excel Converter - PRODUCTION Configuration
# ========================================
# This file is for PRODUCTION (Company Server)
# Database is on company's server

# ========================================
# Server Configuration
# ========================================
PORT=5001
NODE_ENV=production
# Listen on all interfaces
HOST=0.0.0.0

# ========================================
# Database Configuration (COMPANY SERVER)
# ========================================
# IMPORTANT: Update these values with your company server credentials
DB_HOST=your-company-server-ip-or-hostname
DB_PORT=3306
DB_USER=root
DB_PASSWORD=YOUR_PRODUCTION_PASSWORD_HERE
DB_NAME=pdf_excel_converter

# ========================================
# File Upload Configuration
# ========================================
UPLOAD_DIR=./uploads
# Maximum file size in bytes (10485760 = 10MB)
MAX_FILE_SIZE=10485760
# Maximum number of files per batch (1000 recommended)
MAX_FILES_PER_BATCH=1000

# ========================================
# OpenAI Configuration (Optional)
# ========================================
# Leave OPENAI_API_KEY empty to use regex-based extraction only
# AI extraction is more accurate but slower and requires API credits
OPENAI_API_KEY=
OPENAI_MODEL=gpt-4-turbo-preview

# ========================================
# Processing Configuration
# ========================================
# Delay between processing batches (milliseconds)
BATCH_PROCESSING_DELAY=100
# Maximum number of PDFs to process concurrently
MAX_CONCURRENT_PROCESSES=5

# ========================================
# CORS Configuration (PRODUCTION)
# ========================================
# IMPORTANT: Add only your trusted frontend URLs
# Example: http://company-server-ip:3000,https://your-domain.com
ALLOWED_ORIGINS=http://your-company-frontend-url:3000

# ========================================
# Rate Limiting (STRICT for Production)
# ========================================
# Time window in milliseconds (900000 = 15 minutes)
RATE_LIMIT_WINDOW_MS=900000
# Maximum number of requests per time window (lower for security)
RATE_LIMIT_MAX_REQUESTS=100

# ========================================
# Database Backup Configuration
# ========================================
# Directory for database backups
BACKUP_DIR=./backups
# Keep backups for N days (0 = keep forever)
BACKUP_RETENTION_DAYS=30
