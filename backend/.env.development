# ========================================
# PDF to Excel Converter - DEVELOPMENT Configuration
# ========================================
# This file is for LOCAL DEVELOPMENT (Office Laptop)
# Database is on localhost

# ========================================
# Server Configuration
# ========================================
PORT=5001
NODE_ENV=development
# Listen on all interfaces for network access (0.0.0.0)
# This allows other devices on same network to access the server
HOST=0.0.0.0

# ========================================
# Database Configuration (LOCAL LAPTOP)
# ========================================
DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=Ved@1498@!!
DB_NAME=pdf_excel_converter

# ========================================
# File Upload Configuration
# ========================================
UPLOAD_DIR=./uploads
# Maximum file size in bytes (10485760 = 10MB)
MAX_FILE_SIZE=10485760
# Maximum number of files per batch (1000 recommended)
MAX_FILES_PER_BATCH=1000

# ========================================
# OpenAI Configuration (Optional)
# ========================================
# Leave OPENAI_API_KEY empty to use regex-based extraction only
# AI extraction is more accurate but slower and requires API credits
OPENAI_API_KEY=
OPENAI_MODEL=gpt-4-turbo-preview

# ========================================
# Processing Configuration
# ========================================
# Delay between processing batches (milliseconds)
BATCH_PROCESSING_DELAY=100
# Maximum number of PDFs to process concurrently
MAX_CONCURRENT_PROCESSES=5

# ========================================
# CORS Configuration (DEVELOPMENT)
# ========================================
# In development, allow all origins for easy testing
# Add your laptop's IP address for network testing
# Example: http://192.168.1.100:3000,http://192.168.1.101:3000
ALLOWED_ORIGINS=http://localhost:3000,http://localhost:3001,http://localhost:5173,http://127.0.0.1:3000

# ========================================
# Rate Limiting (LENIENT for Development)
# ========================================
# Time window in milliseconds (900000 = 15 minutes)
RATE_LIMIT_WINDOW_MS=900000
# Maximum number of requests per time window (higher for dev)
RATE_LIMIT_MAX_REQUESTS=1000

# ========================================
# Database Backup Configuration
# ========================================
# Directory for database backups
BACKUP_DIR=./backups
# Keep backups for N days (0 = keep forever)
BACKUP_RETENTION_DAYS=30
